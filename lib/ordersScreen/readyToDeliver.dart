import 'package:flutter/material.dart';

class ReadyToDeliver extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ready to deliver Page'),
    );
  }
}
