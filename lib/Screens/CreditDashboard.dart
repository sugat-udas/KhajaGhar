import 'package:flutter/material.dart';

class CreditDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is Credit Dashboard screen'),
    );
  }
}
